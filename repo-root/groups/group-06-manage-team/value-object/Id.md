# Id

**Résumé métier**
Une chaine de caractères

**Attributs**

- value — *UUID*

**Invariants**

- value doit être un UUID valide
- value doit être généré automatiquement lors de la création d'une nouvelle instance de Id

- **Format JSON attendu**

- **Schéma** : `tests/schemas/Id.schema.json`
- **Fixture valide** : `tests/fixtures/Id.valid.json`
- **Fixture invalide** : `tests/fixtures/Id.invalid.json`

**Tests minimaux attendus**

**Génération des fixtures**
