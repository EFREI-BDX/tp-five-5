{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "Business rules not expressible in standard draft-07: birthDate must not be in the future; updatedAt must be greater than or equal to createdAt; statistics.wins + statistics.losses + statistics.draws must be less than or equal to statistics.matchesPlayed; statistics.mvps must be less than or equal to statistics.matchesPlayed.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "firstName": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100
    },
    "lastName": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100
    },
    "email": {
      "type": "string",
      "format": "email"
    },
    "phone": {
      "type": "string",
      "maxLength": 20,
      "pattern": "^(\\+33|0)[1-9]([0-9 .\\-]{8,})$"
    },
    "gender": {
      "type": "string",
      "enum": ["homme", "femme", "non binaire", "non spécifié"]
    },
    "birthDate": {
      "$comment": "Must not be in the future; enforce with domain validation or tests.",
      "type": "string",
      "pattern": "^(0[1-9]|[12][0-9]|3[01])/(0[1-9]|1[0-2])/[0-9]{4}$"
    },
    "height": {
      "type": "number",
      "exclusiveMinimum": 0
    },
    "teamIds": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true
    },
    "statistics": {
      "$comment": "wins + losses + draws must be <= matchesPlayed; mvps must be <= matchesPlayed. Enforce with domain validation or tests.",
      "type": "object",
      "properties": {
        "matchesPlayed": { "type": "integer", "minimum": 0 },
        "goalsScored": { "type": "integer", "minimum": 0 },
        "assists": { "type": "integer", "minimum": 0 },
        "wins": { "type": "integer", "minimum": 0 },
        "losses": { "type": "integer", "minimum": 0 },
        "draws": { "type": "integer", "minimum": 0 },
        "mvps": { "type": "integer", "minimum": 0 }
      },
      "required": ["matchesPlayed", "goalsScored", "assists", "wins", "losses", "draws", "mvps"]
    },
    "status": {
      "type": "string",
      "enum": ["actif", "inactif", "supprimé"]
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "$comment": "Must be greater than or equal to createdAt; enforce with domain validation or tests.",
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["id", "firstName", "lastName", "email", "phone", "gender", "birthDate", "height", "teamIds", "statistics", "status", "createdAt", "updatedAt"],
  "additionalProperties": false
}
