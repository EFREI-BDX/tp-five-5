meta {
  name: Player Joined Team
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/events/teams/player-joined
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-API-KEY: {{apiKey}}
}

body:json {
  {
    "playerId": "{{playerId}}",
    "teamId": "{{teamId}}"
  }
}

tests {
  test("should accept player joined team events", function() {
    expect(res.getStatus()).to.equal(202);
  });
}
