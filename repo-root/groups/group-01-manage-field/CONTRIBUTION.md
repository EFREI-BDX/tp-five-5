# CONTRIBUTION

- Garder les documents courts et factuels.
- Utiliser `snake_case` pour les payloads JSON.
- Mettre a jour `openapi.yaml`, `tests/schemas` et `tests/fixtures` ensemble.
- Toute nouvelle regle metier doit apparaitre dans `domain-summary.md`.
