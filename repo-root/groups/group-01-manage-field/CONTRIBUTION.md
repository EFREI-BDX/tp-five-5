# CONTRIBUTION

- Keep documents short and factual.
- Use `snake_case` for JSON payloads.
- Update `openapi.yaml`, `tests/schemas`, and `tests/fixtures` together.
- Any new business rule must be added to `domain-summary.md`.
