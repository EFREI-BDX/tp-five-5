{
  "$id": "https://example.org/schemas/time-slot.schema.json",
  "title": "TimeSlot",
  "type": "object",
  "$comment": "Business rule enforced in domain code: endAt must be strictly greater than startAt.",
  "properties": {
    "startAt": {
      "type": "string",
      "format": "date-time"
    },
    "endAt": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "startAt",
    "endAt"
  ],
  "additionalProperties": false
}
